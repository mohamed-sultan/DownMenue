import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
  },
  openDrawerButton: {
    marginTop: 50,
    justifyContent: "center",
    alignItems: "center",
  },
  openDrawerText: { fontWeight: "bold", fontSize: 30 },
});
